---
swagger: "2.0"
info:
  version: "1"
  title: "API"
paths:
  /auth/test:
    post:
      operationId: "testAuth"
      parameters: []
      responses:
        default:
          description: "successful operation"
  /dbtest:
    get:
      summary: "Returns a simple select 1"
      description: "Just a select"
      operationId: "testdb"
      produces:
      - "application/json"
      parameters: []
      responses:
        200:
          description: "successful operation"
          schema:
            type: "string"
  /echo:
    post:
      operationId: "echo"
      consumes:
      - "text/plain"
      produces:
      - "text/plain"
      parameters: []
      responses:
        200:
          description: "successful operation"
          schema:
            type: "string"
  /raw:
    put:
      operationId: "raw"
      parameters: []
      responses:
        default:
          description: "successful operation"
  /test/{id}:
    get:
      summary: "Returns a Bean"
      description: "Just a normal endpoint"
      operationId: "test"
      produces:
      - "application/json"
      parameters:
      - name: "id"
        in: "path"
        required: true
        type: "integer"
        format: "int64"
      - name: "limit"
        in: "query"
        required: false
        type: "integer"
        format: "int64"
      - name: "Content-Length"
        in: "header"
        required: false
        type: "integer"
        format: "int64"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ImmutableBean"
definitions:
  ImmutableBean:
    type: "object"
